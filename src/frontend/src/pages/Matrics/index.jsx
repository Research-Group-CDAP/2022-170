import React from "react";
import { PodsList } from "../../components";
const Matrics = (props) => {
  return (
    <div>
      <h1>Matrics Dashboard</h1>
      <PodsList />
    </div>
  );
};

export default Matrics;
