import React from "react";
import {PodsList} from "../../components"

const CpuUsage = (props) => {
	return (
		<div>
			<h1>
				CPU USAGE
			</h1>
			<PodsList/>
		</div>
	);
};

export default CpuUsage;
