import React from "react";
import LineChart from "../LineChart";

const Memory = (props) => {
  return (
    <div>
      <h3>Memory</h3>
      <LineChart />
    </div>
  );
};

export default Memory;
